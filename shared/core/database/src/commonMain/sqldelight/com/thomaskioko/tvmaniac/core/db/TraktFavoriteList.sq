CREATE TABLE trakt_favorite_list (
id INTEGER NOT NULL PRIMARY KEY,
slug TEXT NOT NULL,
description TEXT NOT NULL
);

insertOrReplace:
INSERT OR REPLACE INTO trakt_favorite_list(
id,
slug,
description
)
VALUES(?,?,?);

selectFavorite:
SELECT *
FROM trakt_favorite_list;